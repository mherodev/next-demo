import styled from '@emotion/styled';

const Styled = {
  DivWrapper: styled.div(({ theme }) => ({
    background: 'gold',
    padding: '30px',
  })),
  H1Title: styled.h1(({ theme }) => ({
    fontSize: '2em',
  })),
};
export default Styled;
